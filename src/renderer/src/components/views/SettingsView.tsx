import { PlaceholderView } from './PlaceholderView'

export function SettingsView() {
  return (
    <PlaceholderView
      phase="Phase 2"
      title="Settings"
      blurb="Anthropic API key, model selection, and app preferences — set up during first-run onboarding."
    />
  )
}
