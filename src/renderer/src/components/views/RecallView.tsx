import { PlaceholderView } from './PlaceholderView'

export function RecallView() {
  return (
    <PlaceholderView
      phase="Phase 2"
      title="Recall"
      blurb="Ask in plain language and get a synthesized, cited answer drawn from your campaign notes — with retrieval that works offline."
    />
  )
}
