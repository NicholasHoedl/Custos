import { PlaceholderView } from './PlaceholderView'

export function CaptureView() {
  return (
    <PlaceholderView
      phase="Phase 1"
      title="Capture"
      blurb="Fast, low-friction note-taking during play — NPCs, locations, factions, quests, items, and quotes, all linked to the active session."
    />
  )
}
